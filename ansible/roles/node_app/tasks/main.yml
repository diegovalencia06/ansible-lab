---
# tareas idempotentes básicas
- name: Ensure git is present
  apt:
  name: git
  state: present
  become: yes
- name: Ensure node (and npm) are installed (Debian example)
  apt:
  name: nodejs
  state: present
  become: yes
- name: Include deploy tasks
  import_tasks: deploy.yml
